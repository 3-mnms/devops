apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nginx-admin.fullname" . }}
  labels:
    {{- include "nginx-admin.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.nginxAdmin.replicaCount }}
  selector:
    matchLabels:
      {{- include "nginx-admin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nginx-admin.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.nginxAdmin.image.repository }}:{{ .Values.nginxAdmin.image.tag }}"
          imagePullPolicy: {{ .Values.nginxAdmin.image.pullPolicy }}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: {{ include "nginx-admin.fullname" . }}-config